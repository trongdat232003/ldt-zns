{
  "name": "zns-auto",
  "version": "1.0.0",
  "description": "",
  "main": "test-zns.js",
  "private": true,
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=apps/dashboard",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "node --test packages/**/src/*.test.js",
    "worker:run": "npm start --workspace=apps/worker"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pino": "^9.2.0",
    "pino-pretty": "^11.2.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "eslint": "^9.6.0",
    "prettier": "^3.3.2"
  }
}
